"""reach-guard: strict-mode enforcement wrapper for agent-reach upstream binaries.

Zero runtime dependencies (Python stdlib only, >=3.11). Never modifies
agent-reach or any upstream source/config/storage.
"""

__version__ = "0.1.0"
